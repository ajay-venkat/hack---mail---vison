# ════════════════════════════════════════════════════════════════
#  VisionAid Pro v4 — Requirements
#  Upgrades: MiDaS Depth + DeepSORT Tracking + SUN RGB-D / NYU
# ════════════════════════════════════════════════════════════════

# ── Core Streamlit & Web ─────────────────────────────────────────
streamlit>=1.35.0
streamlit-webrtc>=0.47.0
streamlit-autorefresh>=1.0.1
gTTS>=2.5.0

# ── Vision & ML ──────────────────────────────────────────────────
ultralytics>=8.1.0
opencv-python-headless>=4.9.0
numpy>=1.24.0
Pillow>=10.0.0

# ── PyTorch (MiDaS backbone) ─────────────────────────────────────
# On Streamlit Cloud these are auto-resolved; pin for local repro.
torch>=2.0.0
torchvision>=0.15.0

# ── MiDaS extra dependency ───────────────────────────────────────
timm>=0.9.0

# ── DeepSORT Tracking ────────────────────────────────────────────
deep-sort-realtime>=1.3.2

# ── WebRTC audio/video ───────────────────────────────────────────
av>=11.0.0

# ── Training / Dataset utilities (only for train.py / prepare_dataset.py) ──
scipy>=1.10.0
requests>=2.28.0
tqdm>=4.65.0
PyYAML>=6.0
datasets>=2.14.0        # HuggingFace datasets (NYU Depth V2 mirror)
huggingface-hub>=0.20.0 # HuggingFace Hub CLI helpersfolium
streamlit-folium
requests
